kind = "web"
previewPath = "/"
title = "Python Web Panel V3"
version = "1.0.0"
id = "artifacts/python-panel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = ["/"]
localPort = 8000

[services.development]
run = "cd /home/runner/workspace/panel && pip install -q -r requirements.txt && python app.py"

[services.env]
PORT = "8000"
